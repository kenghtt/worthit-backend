# Local profile overrides.
app.security.cors.allowed-origins=http://localhost:3000
app.turnstile.allowed-hostnames=localhost,127.0.0.1
logging.level.com.worthit.backend=DEBUG

# TODO(worthIt): add local-only overrides (e.g. local DB URL) once those features land.

# Local Postgres (provision yourself; see backend_spec.md section 11 step 1).
# Override SPRING_DATASOURCE_URL / SPRING_DATASOURCE_USERNAME via env vars if your local
# setup differs. Password is read from secrets.properties (spring.datasource.password).
#spring.datasource.url=jdbc:postgresql://localhost:5432/worthit
#spring.datasource.username=worthit_app

# Show SQL during local dev for visibility.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require&tcpKeepAlive=true
#spring.datasource.username=postgres.xoewdorxzbbmoiqbhwmk

spring.datasource.url=jdbc:postgresql://localhost:5432/worthit
#spring.datasource.username=worthit_app
#spring.datasource.password=

app.seed.enabled=false
spring.jpa.hibernate.ddl-auto=update
